# FileEncryptor v1.0.0 发布说明

> 发布产物：`FileEncryptor.exe`（图形界面版）、`enc.exe`（命令行版）
> 平台：Windows x64 · 单文件免安装 · 无需运行库

---

## 简介

FileEncryptor 是一款轻量级文件加密工具，同时提供 **Windows 图形界面** 和 **跨平台命令行** 两个版本。两个版本使用**完全相同的加密算法与文件格式**，加密后的文件可以互相解密。

基于 **AES-256-GCM** 认证加密与 **PBKDF2-HMAC-SHA256** 密钥派生，支持大文件流式处理。

---

## 主要特性

### 加密

* **AES-256-GCM 认证加密**：密文自带认证标签，密码错误与文件被篡改都能被可靠检出（而不是解出乱码）
* **可调加密强度**：PBKDF2 迭代次数 5 万 / 10 万 / 30 万 / 60 万，强度写进文件头，解密时自动读取
* **大文件支持**：4 MB 分块流式处理，内存占用与文件大小无关

### 防护

* **错误密码次数限制**：可设置允许的错误尝试次数，用尽后「永久删除文件」或「禁止解密 N 天」
* **源文件安全删除**：加密后可按设置覆写 1 次 / 3 次再删除，或选择保留
* **解密后直接删除加密文件**：密文不含明文残留，无需覆写

### 图形界面

* **WinUI 3 风格自绘界面**：圆角控件、悬停与进度动画
* **浅色 / 深色 / 跟随系统**三种主题
* **中英文双语**，全局生效（含所有对话框）
* **设置面板**：语言、主题、输出位置、删除策略、完成后行为、加密强度
* **输出位置**：源文件同目录 / 指定目录 / 每次都询问
* **拖拽即用**：把文件（或整个文件夹）拖进窗口即可
* **密码强度提示**：实时彩色分级（很弱 / 一般 / 良好 / 很强）
* **系统级 DPI 感知**：125% / 150% 等缩放下界面清晰不模糊

### 命令行

* 支持 Windows / Linux / macOS
* `-p` 直接传密码，适合脚本与批量处理
* `-o` 指定输出目录、`-k` 保留源文件、`-w` 控制覆写次数、`-t/-x/-l` 配置错误次数限制
* Windows 下**中文路径与中文提示信息完整支持**（内部统一 UTF-8）

---

## 加密算法与文件格式

| 项目 | 说明 |
|---|---|
| 加密算法 | AES-256-GCM（认证加密 / AEAD） |
| 密钥派生 | PBKDF2-HMAC-SHA256，迭代次数可选 |
| 加密强度 | 快速 50,000 / 标准 100,000（默认）/ 安全 300,000 / 极强 600,000 |
| 随机数 | OpenSSL `RAND_bytes`（salt 16 字节，IV 12 字节） |

文件格式（v2，头部 64 字节）：

```
偏移 0    魔数[8]        "FENC\r\n\x1a\n"
偏移 8    版本[1]        = 2
偏移 9    标志[1]        bit0 启用错误次数限制, bit1 模式(0=永久删除, 1=限时锁定)
偏移 10   迭代次数[4]    PBKDF2 迭代次数
偏移 14   最大尝试次数[2]
偏移 16   锁定天数[2]
偏移 18   salt[16]
偏移 34   IV[12]
偏移 46   验证标签[8]    用于快速校验密码，无需解密正文
偏移 54   剩余次数[2]    ← 可变
偏移 56   锁定截止[8]    ← 可变
偏移 64   密文[N]        与明文等长
末尾 16   认证标签[16]
```

偏移 8~54 作为 GCM 的 **AAD** 参与认证：salt、IV、迭代次数与保护策略一旦被改动，解密会直接认证失败。

> **格式不向后兼容**：v1.0.0 之前的构建均为内部验证版本，生成的加密文件（早期 GCM 与最旧的 CBC 格式）**不再支持解密**。请使用 v1.0.0 重新加密。

---

## ⚠️ 安全性说明（请务必阅读）

### 错误密码次数限制的能力边界

该机制把剩余次数与锁定时间写在加密文件头里。这两个字段**必须能在不知道正确密码的情况下被改写**——否则用户输错一次文件就永久打不开了——因此它们**无法被密码学认证**。

这意味着任何人都可以：

* **在尝试前先复制一份 `.enc`，每次失败后还原副本** —— 计数器永远不会归零
* 用十六进制编辑器直接把剩余次数改回初始值、把锁定时间清零

**它是有效的威慑与减速带，但不能对抗有意为之的攻击者。** 请勿把它当作可靠的保密手段，也不要用它来保护高价值数据。

可以确认的一点是：**保护策略本身无法被关闭**。最大次数、模式、锁定天数与 salt / IV / 迭代次数一同纳入 GCM 认证，篡改会导致文件彻底解不开。

### 「永久删除文件」不可逆

若自己忘记密码并连续输错达到上限，文件将被真的删除，**无法恢复**。该选项默认关闭，启用前请确认已另行备份。

### 其他

* 覆写删除对 SSD、云同步目录等介质无法保证 100% 不可恢复
* 密码一旦丢失无法找回（AES-256-GCM 无后门、无恢复机制）
* 建议在本地、非同步目录中使用

---

## 系统要求

| 版本 | 支持情况 |
|---|---|
| **图形界面版** | Windows 10 / 11（x64）。程序在 Windows 7 SP1 上理论上可启动，但窗口圆角、深色标题栏等效果需要 Windows 10 1809+ / Windows 11；**仅 Windows 10 / 11 经过完整验证** |
| **命令行版** | Windows / Linux / macOS，x64。Windows 版静态链接，无外部依赖；Linux/macOS 需要系统 OpenSSL（1.1.0+） |

两个版本均**不需要安装运行库**，也不需要管理员权限。

---

## 使用示例

### 图形界面

1. 运行 `FileEncryptor.exe`
2. 点击「加密文件」选择目标文件（或直接把文件拖进窗口）
3. 设置密码；如需限制错误尝试次数，勾选「输入错误密码 N 次后」并选择用尽后的处理方式
4. 解密时若文件受保护，密码框下方会用红字显示「还能尝试 X 次」

### 命令行

```bat
:: 加密（交互式输入密码）
enc -e -i secret.txt

:: 加密并指定强度、输出目录
enc -e -i secret.txt -p mypass -s extreme -o D:\Encrypted

:: 错 3 次即永久删除文件
enc -e -i secret.txt -p mypass -t 3

:: 错 5 次则禁止解密 7 天
enc -e -i secret.txt -p mypass -t 5 -x lock -l 7

:: 解密（保留加密文件）
enc -d -i secret.txt.enc -p mypass -k

:: 批量解密到指定目录
for %f in (*.enc) do enc -d -i "%f" -p mypass -o D:\Decrypted
```

---

## 已知限制

* **仅提供 x64 版本**。源码与架构无关，32 位版需自行准备 x86 的 OpenSSL 静态库后编译
* 程序未嵌入 `Microsoft.Windows.Common-Controls 6.0` 清单，标准控件按经典样式渲染。因界面控件全部自绘并主动关闭了控件主题，视觉上不受影响
* 错误密码次数限制可被「复制文件后还原」绕过（原理见上）
* 图形界面版仅在 Windows 10 / 11 x64 上做过完整测试

---

## 许可证

MIT License · 仅供学习与安全参考

---

## 反馈

欢迎在 [GitHub Issues](https://github.com/yvziyao/FileEncryptor/issues) 或 [Gitee Issues](https://gitee.com/yvziyao/FileEncryptor/issues) 反馈问题。
